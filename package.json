{
  "name": "entity-explorer",
  "version": "0.0.1",
  "source": [
    "src/index.html",
    "src/map/index.html"
  ],
  "author": "octorock",
  "license": "MIT",
  "devDependencies": {
    "@parcel/transformer-image": "2.0.0-rc.0",
    "@parcel/transformer-sass": "2.0.0-rc.0",
    "gh-pages": "^3.2.3",
    "parcel": "^2.0.0-rc.0"
  },
  "scripts": {
    "start": "parcel",
    "build": "parcel build ",
    "deploy": "rm -r dist/* && parcel build --public-url /entity-explorer/ && gh-pages -d dist"
  },
  "dependencies": {
    "jszip": "^3.7.1"
  }
}
